package Hardware;

import Utils.Pose.*;
import ComputerVision.Camera;

public class Hardware {

// add cameras
   public static Camera cam1 = new Camera(0, 0, true, new Position(0, 0, 0), new Orientation(0, 0, 0)); //TODO set the cam paremeters properly

// add Motor drivers

// add Servos

}
